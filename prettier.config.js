// Normally I hate modifying prettier defaults as they're usually nitpicky, but web3.js tends to use longer names, 
// so we need to increase the print width to avoid single function calls spilling over into multiple lines

export default {
  printWidth: 120,
};
